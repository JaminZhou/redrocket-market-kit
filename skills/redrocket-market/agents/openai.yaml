interface:
  display_name: "RedRocket Market"
  short_description: "Read Red Rocket market context."
  default_prompt: "Use $redrocket-market to gather read-only Red Rocket market context and explain source limits."

policy:
  allow_implicit_invocation: true
